import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.euskalfic',
  appName: 'Euskalfic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
